{
  "name": "business-idea-evaluator",
  "version": "1.0.0",
  "description": "Multi-agent business idea evaluator",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ..",
    "build": "cd frontend && npm run build",
    "start": "NODE_OPTIONS=--openssl-legacy-provider node backend/server.js"
  }
}