{
  "name": "business-evaluator-backend",
  "version": "1.0.0",
  "description": "Backend for business idea evaluator",
  "main": "server.js",
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider node server.js",
    "dev": "NODE_OPTIONS=--openssl-legacy-provider nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  }
}
